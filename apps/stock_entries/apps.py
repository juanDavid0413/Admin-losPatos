from django.apps import AppConfig

class StockEntriesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.stock_entries"
    verbose_name = "Entradas de Stock"
